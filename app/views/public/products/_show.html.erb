<div class="text-center mt-3">
  <%= image_tag product.get_image(250, 250) %>
</div>
<h5 class="my-2 text-center font-weight-bold"><%= product.name %></h5>
<p class="my-3 text-center"><%= product.explanation %></p>
<div>
  <h5 class="my-2 text-center">¥ <%= product.with_tax_price %> (税込)</h5>
</div>
<div class="d-flex justify-content-evenly text-center mb-4">
  <%= form_with model: cart, url: carts_path, method: :post, class: 'ml-md-3' do |f| %>
    <%= f.hidden_field :product_id, value: product.id %>
    <%= f.select :amount, *[1..10], min: 1, include_blank: "個数選択", class: 'mr-md-3' %>
    <%= f.submit "カートに入れる", class: 'btn btn-sm btn-success' %>
  <% end %>
</div>
